<div class="content-wrapper">
  <% if (locals.success && success) { %>
    <div class="alert alert-success fade-out" role="status" aria-live="polite">
      <i class="fas fa-check-circle" aria-hidden="true"></i>
      <%= Array.isArray(success) ? success[0] : success %>
    </div>
  <% } %>

  <% if (locals.error && error && !(locals.success && success)) { %>
    <div class="alert alert-danger fade-out" role="alert" aria-live="polite">
      <i class="fas fa-exclamation-circle" aria-hidden="true"></i>
      <%= Array.isArray(error) ? error[0] : error %>
    </div>
  <% } %>

  <div class="content-section">
    <% if (showTitle) { %>
      <div class="section-header">
        <h2><i class="<%= titleIcon %>"></i> <%= title %></h2>
        <% if (locals.headerActions) { %>
          <div class="header-actions">
            <%- headerActions %>
          </div>
        <% } %>
      </div>
    <% } %>
    <%- content %>
  </div>
</div> 